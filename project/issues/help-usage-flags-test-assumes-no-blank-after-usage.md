The already-realized R-4A0P-7AVY test in `cmd/llm-lint/help_usage_flags_test.go` asserts that `--help` starts with the usage line immediately followed by `flags:` (`lines[1] == "flags:"`). Phase 30 owns only R-TIL4-XZAL and the R-4B8L-L2MN update, and will insert exactly one blank line after `usage:`. That existing assertion is therefore stale relative to D1's current `--help` layout, but it is not one of this phase's owned ids, so gather did not put it in the brief.
